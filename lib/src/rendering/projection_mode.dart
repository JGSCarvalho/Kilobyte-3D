/// Defines how 3D geometry is projected onto the 2D viewport.
enum ProjectionMode {
  
  /// Projects geometry using orthographic projection.
  orthographic,

  /// Projects geometry using perspective projection.
  perspective,
}
